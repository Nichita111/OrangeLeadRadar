# Update Log

## 2026-09-26

* **Update**: Seeding the demo dataset (`S-RUN-03`): [Demo dataset](/architecture/overview.md#demo-dataset) gains the seeded `INCUMBENT_PROVIDER` option labels and a Source plug-ins table (code, rate limit per minute, no daily quota); [api Runtime](/architecture/services/api.md#runtime) states the api also reads `JOB_POLL_INTERVAL_S` and `REFRESH_TARGET_MINUTES` of the worker runtime, for `make refresh-demo`'s wait.
* **Update**: Prospects and Account detail screens (`S-PRO-01`, `S-PRO-02`, `S-PRO-03`, `S-PRO-05`): [ProspectRow](/architecture/interfaces.md#prospectrow) gains `reason`, [Override](/architecture/interfaces.md#override) gains `run_id` (`API-44`, `API-45`), [ScoreView](/architecture/interfaces.md#scoreview) adds each counted finding's `observed_at`, [Conventions](/architecture/interfaces.md#conventions) states repeated array query parameters, `API-39`'s `q` matches as `API-20`, the [Score breakdown](/architecture/rules.md#score-breakdown) names each disqualifier's fact and its null members, [Screen labels](/architecture/services/frontend.md#screen-labels) gain the finding statuses, and [Prospects](/features/prospect-dashboard.md#prospects) and [Account detail](/features/prospect-dashboard.md#account-detail) state their data, empty states, the GDELT link and the breadcrumb parent.
* **Update**: AI gateway (`S-RUN-02`, `S-SIG-04`, `S-SIG-08`): [Runtime](/architecture/overview.md#runtime) specifies the fixture files — path, normalised request, key and response format; the [AI gateway](/architecture/services/worker.md#ai-gateway) states that a call stopped before it is sent writes no `AI_CALL` row, the error each failure raises, Jev's `noul`, `score` and `choice` question mapping, that the highest prompt version is in use with the prompt as system message and the input shape as user message, and `usage.include`; [worker Runtime](/architecture/services/worker.md#runtime) gains `AI_TRANSPORT_BACKOFF_MS`.
* **Update**: Job queue and run lifecycle (`S-PIP-01`, `S-PIP-03`, `S-PIP-04`, `N-05`): the [Job queue](/architecture/services/worker.md#job-queue) states a job whose step has no code fails at once, the `errors` entry a failed job records, and that the job loop enqueues the `SCORE` job of a refresh or reclassification none of whose jobs enqueued it; the [Run lifecycle](/architecture/services/worker.md#run-lifecycle) states a `FETCH` job's `payload` `{plugin_code}`, a refresh with no available plug-in starting at `SCORE`, how claiming sets the run's status, `started_at` and `stage`, and what cancelling writes; [worker Runtime](/architecture/services/worker.md#runtime) gains `JOB_POLL_INTERVAL_S`, and the [api Runtime](/architecture/services/api.md#runtime) reads the source plug-in keys.
* **Update**: [Degradation](/architecture/overview.md#degradation) gains the screen wording of each row, and [Conventions](/architecture/interfaces.md#conventions) the closed set of `details.dependency` values with the Degradation row each names, the database included; `FR-005` shows the screen wording.
* **Update**: Application shell, Sign in and Users: `FR-006` excludes `API-01`; `FR-093` names the `return` parameter; Sign in is two panels (`FR-160`, WF-21); Users shows Never, the password reset and Enable (`FR-095`, `FR-096`, `FR-158`); `FR-007` covers other save errors; Not found (`FR-159`); the Control border token, icon weights and navigation icons (WF-01); relative dates with "just now" and no "today" (`FR-010`, WF-22) and sentence-case enum labels (`FR-011`); the owner confirmed React Bits' licence ([ADR-17](/architecture/adrs/adr-17-animated-components-from-react-bits.md)).
* **Update**: Lead and signal feedback (`S-EVL-01`, `S-EVL-02`): [Feedback effects](/architecture/rules.md#feedback-effects) states a label derived from finding feedback starts `ACTIVE` for the question's current revision, else `STALE`, with `labelled_by` the user who gave the verdict; [Feedback and alerts](/architecture/interfaces.md#feedback-and-alerts) states `API-46` answers `404 NOT_FOUND` when the account has no score for the service yet, as `API-40`; [Run lifecycle](/architecture/services/worker.md#run-lifecycle) states a `SCORE` job's `payload` is `{}`, its scope being its run's `account_id` and `service_id`; [Conventions](/architecture/interfaces.md#conventions) Envelope's `VALIDATION` row now also names a path or query parameter as `field`.
* **Update**: Requirements register: `S-EVL-05`'s Entities gain [`classification`](/architecture/sql-store.md#classification), the only link from a finding to the run that created it; [Impact](/architecture/rules.md#impact)'s Inputs name it the same way, and its algorithm row for `precision` and `labelled_items` says the latest passing evaluation orders by `evaluation_result.created_at`.
* **Update**: Sign-in, roles, user management and the audit trail (`S-SEC-01` to `S-SEC-03`, `N-07`, `S-AUD-01`), decisions G1 to G8: [Runtime](/architecture/overview.md#runtime) gains the two database roles, the owner through `MIGRATION_DATABASE_URL` and the application role `leadradar_app` through `DATABASE_URL` with `APP_DB_PASSWORD`, holding only `SELECT` and `INSERT` on [`audit_event`](/architecture/sql-store.md#audit_event); [api Runtime](/architecture/services/api.md#runtime) gains `MIGRATION_DATABASE_URL` and `APP_DB_PASSWORD` and restates `DATABASE_URL` and `POSTGRES_PASSWORD`; [Constraints and indexes](/architecture/sql-store.md#constraints-and-indexes) states each migration grants the application role its privileges; [Demo dataset](/architecture/overview.md#demo-dataset) Users gain the email's local part as display name; [`app_user`](/architecture/sql-store.md#app_user) `failed_logins` and `locked_until` state the reset on lock; [Audit actions](/architecture/sql-store.md#audit-actions) gains the changed-fields convention and the `LOGIN_FAILED` reason wording, and [`audit_event`](/architecture/sql-store.md#audit_event) `actor_id` names a failed sign-in and seeding as null-actor cases; [UserUpdate](/architecture/interfaces.md#userupdate) `password` and the `API-06` note state the minimum length applies to a reset that changes only the hash; [Conventions](/architecture/interfaces.md#conventions) Authentication names the `leadradar_session` cookie's `Path` and `Max-Age`, and its `LOCKED` envelope row states `retry_after_min`; the [Authentication and users contracts](/architecture/interfaces.md#authentication-and-users-contracts) notes state `API-04`'s ordering, `API-05`'s `200`, and a no-op `PATCH` writing no audit row; the [Python guidelines](/guidelines/python.md#style) name `argon2-cffi`'s `PasswordHasher` for password hashing.
* **Update**: Runtime stack foundation (`S-RUN-01`, `N-12`): [Constraints and indexes](/architecture/sql-store.md#constraints-and-indexes) gains a partial unique index of one `DISCOVERY` `pipeline_run` per service and restates the range check as "every column whose type states a 0–1 or 0–100 range is within it"; [Health](/architecture/interfaces.md#health) states each check's call (`database` runs `SELECT 1`; `embedder` calls `GET {EMBEDDER_URL}/health`; `classifier` and `llm` are `NOT_CONFIGURED` without `OPENROUTER_API_KEY`, else each calls `GET {OPENROUTER_BASE_URL}/key`); [api Runtime](/architecture/services/api.md#runtime) gains `POSTGRES_PASSWORD`, and [overview Runtime](/architecture/overview.md#runtime) names the database password among secrets; [worker Runtime](/architecture/services/worker.md#runtime) states the worker also reads `DATABASE_URL` and `LOG_LEVEL` of the api runtime; the [Python](/guidelines/python.md#toolchain) and [TypeScript](/guidelines/typescript.md#toolchain) toolchains drop the `*planned*` marker from `validate`, now implemented.
* **Update**: Requirements register: `N-12` and `AC-67` are scoped to the `api` and `worker` logs, carrying the request id on a line written while serving a request and the run id on a line written for a run.
* **Update**: Pre-implementation review: the demo refresh is `make refresh-demo` ([Seeding](/architecture/overview.md#runtime)); `classification.evidence_retried`, the gate values on `evaluation_result`, `ProspectPage.band_counts`, `LabelQueue`, `ScoreView.last_crm_sync` and the preview's hint terms and source types are added; a manual label replaces a feedback label; a service is scored from its first activation; frontend keys reach the client through `/config.json`; replay needs no provider key and its health reads the recordings; Degradation covers long pasted text and persona mapping; `API-71` to `API-77` join the api service; screens list the contracts they read; the P0 gate criteria no longer depend on P1 features.
* **Update**: The [FL-06](/features/accounts-and-discovery.md#fl-06-discover-and-accept-suggested-accounts) diagram shows the api queueing the discovery run and the worker storing the candidates in the database; the worker never answers the api.
* **Update**: Fixes before merging: the Prospects drawer shows the demo scores of the list (Lufthansa Group 58, Kuehne+Nagel 53), the seeded [industries](/architecture/overview.md#demo-dataset) carry short labels, and a draft that names a retired industry says so and must drop it before saving (`FR-030`, `FL-22`).

## 2026-09-25

* **Update**: The configurable-scope decisions are [ADR-18](/architecture/adrs/adr-18-industries-and-markets-as-configuration.md) and [ADR-19](/architecture/adrs/adr-19-source-provider-terms-and-limits.md), the Industries and markets screen is `FR-154` to `FR-156` with `WF-26`, and the Impact panel is `FR-157`, renumbered when merging `main`, where `ADR-17`, `FR-101` to `FR-153` and `WF-24` to `WF-25` were already taken.
* **Update**: Everything in the brief's solution scope is configurable: industries and markets are Admin-maintained lists ([ADR-18](/architecture/adrs/adr-18-industries-and-markets-as-configuration.md); `industry`, `market`, `API-71` to `API-76`, the Industries and markets screen, `FL-22`, `B-39`, `S-CFG-07`, `AC-74`), a market stored as its countries. An impact report ([Impact](/architecture/rules.md#impact), `API-77`, `FR-157`, `B-40`, `S-EVL-05`, `AC-75`) and the [Demo walkthrough](/architecture/overview.md#demo-walkthrough) are added. Provider terms are recorded ([ADR-19](/architecture/adrs/adr-19-source-provider-terms-and-limits.md)): GDELT pacing and credit, no Google News feed, no Crunchbase key expected. Contradictions resolved: `next_refresh_at` and rescore-run ownership, one `SCORE` job per rescore, the Runs details role, literal numbers now configuration keys, the commit policy for document changes, `AC-11` without a Crunchbase recording, rules examples set up through REST, the Prospects wireframe, the disqualifier term, and which label keeps a purged passage's text.
* **Update**: The animated-components decision is [ADR-17](/architecture/adrs/adr-17-animated-components-from-react-bits.md), renumbered when merging `main`, where ADR-15 and ADR-16 were already taken by OpenRouter and question-scoped passage selection.
* **Update**: The eight [features](/features/index.md) follow the mockup: a Prospects drawer over the list, filter counts and legends, running and verdict states on Account detail, Alerts states, import stepper and check, discovery states, plug-in availability, Labelling progress, quality gate wording, two-pane Service editor, Scoring side panel, sign-in hints and audit kind filter (`FR-129` to `FR-153`, `WF-25`); no register row changes.
* **Update**: The [frontend](/architecture/services/frontend.md) gains page anatomy, visual language with light and dark design tokens, score presentation with `WF-24`, messages and dialog rules, and a Motion section built on React Bits (`FR-101` to `FR-128`), the user menu becomes a user card in the navigation (`FR-004`, `WF-01`), and [ADR-17](/architecture/adrs/adr-17-animated-components-from-react-bits.md) records the animated components; the [TypeScript guidelines](/guidelines/typescript.md) follow.
* **Update**: Question-scoped hybrid passage selection ([ADR-16](/architecture/adrs/adr-16-question-scoped-hybrid-passage-selection.md)): a document of at most `WHOLE_DOCUMENT_MAX_CHARS` is one passage, a longer one is split at its sections into passages with a `section` path, every passage is read with a passage header, and each question retrieves its own `PASSAGES_PER_QUESTION` passages of a long document by keyword on its hint terms (`chunk.lexemes`) fused with embedding similarity (`RETRIEVAL_CANDIDATES`, `RETRIEVAL_RRF_K`), capped by `MAX_PASSAGES_PER_DOCUMENT`; reclassification retrieves for the changed question; the label queue gains a not-selected stratum and the quality check reports `missed_evidence` and per-source-type results (`S-ING-04`, `S-SIG-07`, `S-EVL-03`, `AC-18`, `AC-48`, `AC-71` to `AC-73`).
* **Update**: `S-SIG-08` again keeps classification by Jev running while the LLM budget is exhausted, and the [Budget guard](/architecture/rules.md#budget-guard) caps LLM calls — OpenRouter chat completions — not Jev's Decisions calls; `B-38` and `S-SIG-10` are retired; the [degradation](/architecture/overview.md#degradation) table separates an OpenRouter outage, which stops Jev too, from an exhausted budget, which does not; ADR-02 is trimmed of the Jev key clause that [ADR-15](/architecture/adrs/adr-15-openrouter-as-the-llm-provider.md) supersedes; operational complexity takes the higher level when its measures disagree; `API-36`'s roles column states that only an Admin cancels reclassify, rescore and quality-check runs.
* **Update**: Jev is served by OpenRouter's Decisions API with the OpenRouter key (`JEV_MODEL`, `JEV_DECISIONS_URL` replace `JEV_API_KEY`, `JEV_BASE_URL` and `JEV_PRICE_EUR_PER_CALL`), so `AC-23` verifies `S-SIG-04` inside the release gate and `S-SIG-10` keeps only Jev classifying under an exhausted budget; `operational_complexity` levels are defined by countries and business units; an import that changes an attribute rescores the account; a finding of a `CHOICE` question records its `option_key`, shown on Account detail.
* **Update**: The LLM provider is OpenRouter instead of Anthropic ([ADR-15](/architecture/adrs/adr-15-openrouter-as-the-llm-provider.md)): `OPENROUTER_API_KEY` and `OPENROUTER_BASE_URL` replace `ANTHROPIC_API_KEY`, the model keys hold OpenRouter model ids with no default, a call's cost is OpenRouter's reported `usage.cost` so `LLM_PRICES_EUR_PER_MTOK` is removed, and the audit `provider` value `ANTHROPIC` becomes `OPENROUTER`.
* **Update**: The Jev adapter is split into `B-38` and `S-SIG-10` (P1, verified by `AC-23` and the new `AC-70`), so the release gate no longer needs a Jev key, and `AC-27` tests the budget with the LLM adapter; the seed imports the accounts from the [demo account file](/architecture/overview.md#demo-dataset) with their careers, newsroom and report addresses, size and complexity, `AccountImportRow` gains `operational_complexity`, and `AC-10` expects the seeded accounts as updated; replay reads the clock from `CLOCK_FILE`; an inactive or new question counts only through an activated scoring version; only an Admin cancels reclassification, rescore and quality-check runs; `outreach_draft` `contact_id` is `ON DELETE SET NULL` and `classification` `strength` is null while pending; quotes are matched with typographic characters normalised and stored as the passage writes them; the Budget guard covers the LLM classifier adapter and names `LLM_PRICES_EUR_PER_MTOK`.
* **Update**: By owner decision contacts are entered by people only (the `contact` `origin` column is removed, [ADR-10](/architecture/adrs/adr-10-minimal-contact-data.md)); scheduled refresh (`B-15`), source detection (`B-36`), profile enrichment (`B-37`) and the moderated walkthrough (`N-13`, `AC-68`) are P1, with `AC-69` split from `AC-11`; a parent's view never combines subsidiaries' signals, Scoring settings stay Admin-only, and the Labelling screen offers no translation.
* **Update**: [Document conventions](/guidelines/documents/common.md#tooling) state the checker's cross-document rules — cited identifiers, numbering, priority inheritance, the release gate and tags — and every file's `tags` now equal the features whose reading order links it.
* **Update**: The bundle is created for spec-driven development of LeadRadar from the [challenge brief](/reference/challenge-brief.md) and [Annex 1](/reference/annex-1-participant-reference-pack.md): requirements (`RULE-01`–`RULE-10`, `B-01`–`B-35`, `SC-A`–`SC-D`, `S-` and `N-` rows with flows and entities, `AC-01`–`AC-67`), the kernel (SQL store, rules, interfaces `API-01`–`API-70`, overview with the demo dataset), the api, worker and frontend services, eight features with flows `FL-01`–`FL-21`, `ADR-01`–`ADR-14`, guidelines, the generated traceability matrix and the checker.
