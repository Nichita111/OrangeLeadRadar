// No screen is in scope of this task ([Not building](/.work/stack-foundation/design.md));
// `App` is the mount point later routes and the shell attach to.
export function App(): null {
  return null;
}
