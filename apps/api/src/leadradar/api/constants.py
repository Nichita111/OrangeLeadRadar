"""The one base path of [Conventions](/architecture/interfaces.md#conventions) "Transport and
base path", shared by the routers that need it in a cookie's `Path` and by `app.py`'s mount."""

from __future__ import annotations

API_PREFIX = "/api/v1"
