# Task: <title>

- Slug: `<slug>`
- Branch: `<type>/<feature-slug>-<slug>` from `<base branch>` at `<base sha>`
- Feature: `<feature-slug>`
- Requested: `<the /implement arguments, verbatim>`

## Scope

Requirement rows, copied from `docs/requirements/traceability.md`:

| Row | Priority | Flows | Entities | Acceptance |
|---|---|---|---|---|
| `S-XXX-nn` | P0 | `FL-nn` | `table` | `AC-nn` |

Screen rows: `FR-nnn`, … on <screen> (`docs/features/<feature>.md#<screen>`), or none.

## Acceptance criteria in scope

`AC-nn`, …

## Deferred

Criteria in scope that this task cannot verify yet, each with its reason and the task that will provide the missing surface.

## Decisions

Human decisions, newest last, each dated: the question, the answer, and who decided.

## Status

- [ ] Design written
- [ ] Design approved
- [ ] Built
- [ ] QA run
- [ ] Critic passed
- [ ] Committed

Rounds: one line per fix round — what failed, what was fixed.
